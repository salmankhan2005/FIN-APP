# Project Rules

- **Auto-Deploy to GitHub**: Whenever you make any code changes in this workspace, ALWAYS commit and push them to the GitHub repository automatically using `git add .`, `git commit -m "..."`, and `git push`. The user's live deployment pipeline (Vercel/Render) relies entirely on GitHub pushes to stay updated. Do not ask for permission before pushing, just do it after modifying code.
