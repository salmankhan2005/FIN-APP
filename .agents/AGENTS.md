# Project Rules

- **Auto-Deploy to GitHub**: Whenever you make any code changes in this workspace, ALWAYS commit and push them to the target repository (`salmankhan2005/FIN-APP` / `origin`) automatically using `git add .`, `git commit -m "..."`, and `git push origin master:main`. Never push to `jeevaa` (JEEVAA0107/Finance-App).
