# Project Rules

- **Auto-Deploy to GitHub**: Whenever you make any code changes in this workspace, ALWAYS commit and push them to the target repository (`salmankhan2005/FIN-APP` / remote `finapp`) automatically using `git add .`, `git commit -m "..."`, and `git push finapp master:main`. Never push to `JEEVAA0107/Finance-App` (origin).
